# -*- coding: utf-8 -*-
"""
包装模拟 · 参数表（这就是"慢慢往里填"的地方）
============================================
每个值都可调。带 ★待确认 的是现在还用占位值、需要你换成真实数字的。
换好一个，就把它的名字从最下面的 PENDING 列表里删掉——引擎每次会提示还剩哪些没填。
零件尺寸(PARTS)是 STEP 内核实测，已定稿，一般不用动。
"""

# ---------------------------------------------------------------
# 1) 零件：STEP 内核实测净尺寸 (长L, 宽W, 高H, cm；重量kg 可选 None)
#    ✅ 已确认，来源见项目记忆 project-gea-dingpeng
# ---------------------------------------------------------------
PARTS = [
    #  名称           长L    宽W    高H    重量kg
    ("前框架 FR",   103.0, 36.2,  9.3,  None),   # ★待确认: 单件重量
    ("后框架 RR",   103.0, 16.1,  9.3,  None),   # ★待确认: 单件重量
    ("左框架 LH",   113.0, 11.0,  9.7,  None),   # ★待确认: 单件重量
    ("右框架 RH",    98.6, 11.0,  9.7,  None),   # ★待确认: 单件重量
]

# ---------------------------------------------------------------
# 2) 货车车厢
# ---------------------------------------------------------------
TRUCK_INNER_CM   = (420.0, 190.0, 190.0)  # ★待确认: 车厢内尺寸(长,宽,高 cm)。默认≈4.2米厢货
TRUCK_PAYLOAD_KG = 1500.0                 # ★待确认: 车厢最大载重 kg（不限重就填很大）
TRUCK_COST_YUAN  = 1500.0                 # ★待确认: 每趟(每车)运费 元
TRUCK_FILL_EFF   = 0.90                   # 车厢装载利用率(码放空隙损失) 0~1，一般 0.85~0.92

# ---------------------------------------------------------------
# 3) 纸箱
# ---------------------------------------------------------------
CARDBOARD_YUAN_PER_M2 = 8.0    # ★待确认: 瓦楞纸板单价 元/m²(按箱外表面积算料费)
PART_CLEARANCE_CM     = 1.0    # 每个零件四周缓冲(单边 cm)
CARTON_WALL_CM        = 0.5    # 纸箱壁厚(加到外尺寸)
MAX_CARTON_EDGE_CM    = 130.0  # 单箱最长边上限(太大不好搬)
MAX_PARTS_PER_CARTON  = 60     # 单箱零件数上限(限制暴力循环规模)

# ---------------------------------------------------------------
# 4) 订单量：发多少「套」(1套 = 1×FR+1×RR+1×LH+1×RH)
# ---------------------------------------------------------------
SETS_TO_SHIP = 1000            # ★待确认: 本次发运套数

# ---------------------------------------------------------------
# 5) 报告输出位置
#    留空 "" = 和脚本/config 同一个文件夹（默认）。
#    也可填子文件夹名(如 "outputs")或绝对路径；不存在会自动创建。
# ---------------------------------------------------------------
OUTPUT_DIR = ""

# ---------------------------------------------------------------
#  还没用真实数据确认的参数——确认一个就从这删一个
# ---------------------------------------------------------------
PENDING = [
    "PARTS 单件重量(4件)",
    "TRUCK_INNER_CM 车厢内尺寸",
    "TRUCK_PAYLOAD_KG 车厢限重",
    "TRUCK_COST_YUAN 每趟运费",
    "CARDBOARD_YUAN_PER_M2 纸板单价",
    "SETS_TO_SHIP 发运套数",
]
